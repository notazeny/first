# 黑马记账（Heima Bookkeeping）

## 项目简介

黑马记账是一款运行在 Windows 和 macOS 电脑上的本地记账桌面应用，帮助用户记录每一笔收入与支出（单位：人民币），并通过两级分类让用户清楚掌握钱的去向。所有数据仅保存在用户本机，不联网、不上传。

- 运行平台：Windows 10/11、macOS
- 界面语言：中文
- 开发/沟通语言：中文

## 核心功能（第一版）

1. **记一笔**
   - 类型：支出 / 收入
   - 金额：人民币（元），支持小数点后 2 位，上限 9,999,999.99 元
   - 分类：一级大类 + 二级小类（必选，两级联动选择）
   - 日期：默认今天，可修改
   - 备注：可选
2. **明细列表**
   - 按时间倒序展示所有记录（类型、分类、金额、日期、备注）
   - 可按月份筛选查看
3. **编辑 / 删除**
   - 可修改或删除任意一条已有记录
4. **设置**
   - 界面语言：中文 / English（默认中文），切换立即生效并自动记住
5. **统计**
   - 月度汇总：近 12 个月每月支出/收入柱状图（支出/收入可切换）
   - 分类占比：选定月份的一级分类环形图（月份、支出/收入可切换）

## 分类体系（内置两级分类）

### 支出分类

| 一级分类 | 二级小类 |
|---|---|
| 🍚 餐饮 | 早餐、午餐、晚餐、夜宵、零食饮料、外卖、聚餐应酬、食材 |
| 🚌 交通 | 公交地铁、打车、加油充电、停车费、火车高铁、机票、共享单车 |
| 🛍️ 购物 | 服饰鞋包、日用品、数码电器、美妆护肤、母婴、其他购物 |
| 🏠 居住 | 房租、房贷、水电燃气、物业费、家具家电、维修 |
| 📱 通讯 | 话费充值、宽带网络、快递邮寄 |
| 💊 医疗健康 | 门诊、药品、住院、体检、保健品 |
| 📚 教育 | 学费培训、书籍资料、文具、考试报名 |
| 🎮 娱乐 | 电影演出、游戏充值、会员订阅、旅游度假、运动健身、宠物 |
| 🎁 人情 | 请客送礼、红包、捐款、孝敬父母 |
| 📦 其他 | 其他杂项 |

### 收入分类

| 一级分类 | 二级小类 |
|---|---|
| 💰 工资 | 工资、奖金、补贴报销 |
| 💼 兼职副业 | 兼职收入、自由职业 |
| 📈 投资理财 | 利息收入、理财收益 |
| 🧧 红包礼金 | 红包、礼金 |
| 📦 其他 | 其他收入 |

## 产品规则

- 金额单位固定为人民币；界面默认中文，可在"设置"页切换为 English
- 分类为内置两级分类，暂不支持用户自定义（列入后续版本）
- 数据仅保存在本机，不联网

## 技术栈（2026-08-18 由用户拍板决定）

| 项目 | 选型 | 说明 |
|---|---|---|
| 应用形态 | Electron 桌面应用 | 后续打包为 Windows 安装包 / Mac 应用 |
| 界面技术 | Vue 3 | 使用 electron-vite 构建 |
| 数据存储 | SQLite 数据库（Electron 内置引擎） | 使用 Electron 自带的 SQLite 引擎（node:sqlite），无需额外安装；单文件数据库存放于应用数据目录 |
| 图表 | ECharts | 统计页柱状图/环形图（2026-08-18 用户拍板） |
| 开发语言 | JavaScript | |

## 项目结构规划（2026-08-19 补充）

整个项目像一栋房子，每个文件夹有固定分工。以后加新功能时，代码放进对应的"房间"即可。

```
heima-jizhang/                     项目根目录
├── CLAUDE.md                      产品文档 + 开发规则（你手上这份）
├── package.json                   项目档案：名称、版本、依赖零件清单、打包配置
├── package-lock.json              零件清单的精确版本快照（自动生成，勿手改）
├── electron.vite.config.mjs       构建规则：把源码组装成可运行程序的配置
├── build/                         打包素材
│   └── icon.png                   应用图标（蓝底白¥，由脚本生成）
├── scripts/                       辅助脚本
│   └── make-icon.js               图标生成脚本（改图标设计后重跑再打包）
├── src/                           源代码（主体，所有功能都写在这里）
│   ├── main/                      ① 后台管家：数据库、创建窗口、系统能力
│   │   ├── index.js               程序入口：建窗口、注册与界面的通信通道
│   │   └── db.js                  数据库代码：建表、账目增删改查
│   ├── preload/
│   │   └── index.js               ② 传话桥梁：界面与后台之间唯一的安全通道
│   └── renderer/                  ③ 前台界面：用户看到和点击的一切
│       ├── index.html             界面入口页面
│       └── src/
│           ├── main.js            界面启动代码
│           ├── App.vue            页面框架：顶部品牌栏 + 记账/统计/设置三个标签
│           ├── i18n.js            中英文词表与翻译工具
│           ├── data/
│           │   └── categories.js  内置两级分类定义（支出 10 类 / 收入 5 类）
│           └── views/             三个页面，一个文件一个页
│               ├── RecordView.vue     记账页（记一笔 + 明细列表）
│               ├── StatsView.vue      统计页（柱状图 + 环形图）
│               └── SettingsView.vue   设置页（界面语言切换）
├── out/                           构建中间产物（自动生成，勿手改）
├── dist/                          打包成品（安装包、便携版 exe 都在这里）
└── node_modules/                  第三方零件仓库（自动生成，勿手改）
```

- Electron 应用天生分"三班人"：**main 后台**（碰数据库、管窗口）、**renderer 前台**（画界面）、**preload 桥梁**（后台与前台之间的安全传话通道，界面要数据、存数据都从这里走）
- 文件归类口诀：**新页面 → views/；新的中英文文案 → i18n.js；涉及数据存储的改动 → main/db.js**
- 三个自动生成的目录（node_modules、out、dist）不要手动改；动手只动 src/ 与 scripts/

## 开发环境注意事项（踩坑记录）

- **在 Claude Code 的终端沙箱中运行 `npm run dev` 前，必须先执行 `unset ELECTRON_RUN_AS_NODE`**：沙箱环境注入了该变量，会导致 Electron 以纯 Node 模式启动（界面接口失效）。用户自己的终端不受影响。
- npm 已配置国内镜像（registry.npmmirror.com）；重装依赖时若 Electron 本体下载失败，需加环境变量 `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`。
- 本机没有 Visual Studio C++ 编译工具，且 GitHub 访问受限——凡需要"机器码零件"的模块（如 better-sqlite3）不可用；数据库一律使用 Electron 内置的 node:sqlite（SQLite 3.53.1，已实测可用）。

## 技术实现备忘

- 数据表：`records`（收支记录；金额以"分"为单位的整数存储，避免小数误差）、`settings`（键值设置）
- 界面与主进程通过 IPC 通道通信：`records:list/add/update/remove`、`settings:get/set`（接口清单见 src/preload/index.js）
- 界面语言切换使用项目内置词表（src/renderer/src/i18n.js），未引入第三方库
- 两级分类定义在 src/renderer/src/data/categories.js；记录中存分类标识（如 food.breakfast），显示时按界面语言翻译
- Windows/Linux 已去掉 Electron 默认英文菜单；macOS 保留最小中文菜单
- 统计页（src/renderer/src/views/StatsView.vue）：数据复用 listRecords 全量查询后前端聚合；分类配色使用经色盲安全校验的规范调色板（固定顺序分配，见组件内 CATEGORY_COLORS），环形图最多展示前 7 大分类，其余归并"其他"

## 打包说明（2026-08-19 首次产出安装包）

- 打包工具：electron-builder；命令 `npm run build:win`，产物在 dist/：`黑马记账 Setup 0.1.0.exe`（安装版）+ `黑马记账 0.1.0.exe`（免安装便携版）
- 首次打包需下载安装包制作工具，国内网络需设置 `ELECTRON_BUILDER_BINARIES_MIRROR=https://npmmirror.com/mirrors/electron-builder-binaries/`
- 应用图标：scripts/make-icon.js 程序化生成 build/icon.png（蓝底白¥，512×512），修改设计后重跑脚本再打包
- 打包后程序进程名为「黑马记账.exe」；沙箱环境测试打包版同样需先 `unset ELECTRON_RUN_AS_NODE`
- 开发模式与安装版共用同一数据目录（%APPDATA%\黑马记账），账目数据无缝延续
- 未购买数字签名证书：安装时 Windows 可能提示"未知发布者"，点"更多信息 → 仍要运行"即可（属正常现象）
- macOS 安装包（dmg）须在 Mac 电脑上执行 `npm run build:win` 对应命令生成（苹果限制），代码无需改动

## 项目执行计划

### ✅ 已完成（第一版 v0.1.0）

| 阶段 | 内容 | 完成日期 |
|---|---|---|
| 一、基础记账 | 记一笔、明细列表、编辑/删除、设置（中英文切换）、SQLite 本地存储 | 2026-08-18 |
| 二、统计图表 | 月度汇总柱状图 + 分类占比环形图（ECharts） | 2026-08-19 |
| 三、打包发布 | Windows 安装包 + 免安装便携版（均在 dist/） | 2026-08-19 |

### ⏳ 当前进行中

- 用户安装试用安装包，反馈问题 → 有问题则修复后重新打包（版本号相应递增）

### 📋 后续版本候选功能（按建议优先级排序，最终由用户决定）

1. **数据导出 / 备份 / 导入**（建议优先）：把账目存成文件，防电脑故障丢数据，也方便换电脑搬家
2. **关键字搜索**：账目多了以后按备注/金额/分类快速找账
3. **预算提醒**：给每月设定支出上限，超支时提醒
4. **自定义分类**：允许增删自己的分类（当前为内置固定分类）
5. **macOS 安装包**：需在 Mac 电脑上执行打包命令（苹果限制，代码已兼容，无需改动）

> 每项开工前，先由 Claude 提产品设计 + 技术方案，用户确认后再开发（见协作规则）。

### 🔁 日常开发流程

1. Claude 提出功能设计/技术方案，列明选项 → 用户拍板
2. Claude 写代码 → 重启应用自测（沙箱内先 `unset ELECTRON_RUN_AS_NODE`）
3. 用户实际试用确认 → 完成；同时把重要决定与踩坑记录更新进本文件

## ⚠️ 协作规则（必须全程遵守）

1. **技术决策必须由用户拍板**：用户不懂编程技术。凡是涉及技术方案、工具、库、架构等任何技术层面的选择，Claude 必须：
   - 列出 2～4 个可行方案；
   - 用通俗语言解释每个方案的优缺点（少用术语，必须出现术语时要附带解释）；
   - 给出推荐意见及理由，但**最终由用户决定**；
   - 不得擅自替用户做技术决定，不得假定用户"默认同意"。
2. **沟通方式**：用通俗易懂的中文解释，多用生活化打比方；用户不懂的术语要当场解释。
3. **文档语言**：项目内所有文档、代码注释、界面文案一律使用中文。
4. **需求变更**：用户提出的新需求若涉及技术取舍，同样按规则 1 执行。
5. 产品功能设计由 Claude 提议、用户确认后再开发。
